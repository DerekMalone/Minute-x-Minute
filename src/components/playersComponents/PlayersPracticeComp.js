import React, { useEffect } from 'react';
// import PropTypes from 'prop-types';

const PlayersPracticeComp = () => {
  useEffect(() => {
    console.warn('You are on the Players Practice Component');
  }, []);

  return <div>Component for the Players Practice.</div>;
};

// PlayersTeamComp.propTypes = {

// }

export default PlayersPracticeComp;
